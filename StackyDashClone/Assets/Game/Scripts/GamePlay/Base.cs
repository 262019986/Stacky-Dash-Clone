using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Base : MonoBehaviour 
{
  
    void Start()
    {
        //transform.localScale = new Vector3(transform.localScale.x , LevelManager.Instance.Length1 , transform.localScale.z );
        //transform.position = new Vector3(transform.position.x , transform.position.y + LevelManager.Instance.Length1/2 , transform.position.z );
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
